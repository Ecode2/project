# Backend REST + WebSocket base URLs (set these in Vercel too).
NEXT_PUBLIC_API_BASE=https://bookverse.api.ecode2.com
NEXT_PUBLIC_WS_BASE=wss://bookverse.api.ecode2.com

# Legacy alias still honoured as a fallback for NEXT_PUBLIC_API_BASE.
# NEXT_PUBLIC_API_URL=
